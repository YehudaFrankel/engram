# Todo

Current tasks in priority order. Update at Start Session and End Session.

---

| Priority | Task | Status | Notes |
|----------|------|--------|-------|
| Medium | ChromaDB backend + MCP server for memory search | Not Started | ChromaDB replaces pkl index (persistent, auto-updates); MCP server exposes search_memory tool so Claude calls it directly instead of via Bash. Both in one phase. |

---

## How to use this file

- **High** — do this session if at all possible
- **Medium** — do this week
- **Low** — do when High and Medium are clear

Move completed tasks to Done, then remove them at End Session.
If a task is Blocked — note what's blocking it. Don't leave it invisible.

This file is read at Start Session. Keep it honest — it's useless if it doesn't reflect reality.
